# Grok 4.6 on the audit branch

Date 2026-09-01. Run headless via `fledge rune run -- grok -p`, 1036 seconds,
against `review/audit-verification`. One of three independent passes over the
same branch, each given the same prompt and none shown the others' answers.

Kept unedited, including the parts that are wrong and the parts this repository
then acted on. The prompt asked each model to try to refute the branch's own
new finding, to look for what two same-family passes would not think to check,
and to end with a confidence number it was told to keep honest rather than
agreeable.

**Confidence: 52 / 100.** What was done about it is in
[`2026-09-01-opus-5-audit-verification.md`](2026-09-01-opus-5-audit-verification.md),
section 7.

---

I'll start with the repo conventions, review docs, and the branch diff, then try to refute the lateness-for-sale finding on LocalNet.Next I’ll read the two audit documents, the keeper contract, and the branch diff.The branch has three commits versus `main`; I’ll pull the full diff and the keeper contract next.I’ll pull the code diffs and start the LocalNet spike plus the attack suite in parallel.The spike and unit tests are running; I’ll read the new fallback test and the attack suite while they execute.The spike reproduced: 25,600 paid, +21,600 to the attacker. Next I’ll probe a worse non-self-healing variant and check the code the two passes didn’t.The reference bot backs off a full interval after a target revert, which may remove the race the verification relies on. I’ll measure the worse variants on LocalNet.The two-upkeep numbers may be inflated by leftover probe state. I’ll rerun those cases on a fresh probe.The mechanism is real. I reproduced it on LocalNet: creator paid 25,600 µALGO, attacker netted +21,600 µALGO on a 1,000 µALGO outlay. I could not make the extraction unprofitable. What I could refute are the two limits the verification treats as making this “advice”: that the attacker must win a race with polling keepers, and that it self-heals within two cycles. Both are false in conditions this repo actually ships.

`main..HEAD` is three commits, not two. `61d9a5a` (CEPY52VZRWFL) is on the branch as well as the audit and the verification. Keeper `contract.py` is unchanged. 535 unit tests passed. `scripts/attacks.py` refused all 3 on LocalNet. I did not query TestNet, so I did not re-count “7 of 33” or the 54.217 ALGO solvency line.

---

## 1. Manufactured lateness — could not refute the extraction, did refute the limits

**Path.** `execute` (`contract.py` 390–400): if `fee_cap > fee_per_execution` and `due > last_serviced_round`,

```
lateness = round - last_serviced
excess   = min(max(lateness - interval, 0), interval)
fee      = base + (cap - base) * excess // interval
```

then the inner call to the target, then `itxn.Payment` to `Txn.sender`. A rejected inner call reverts the group; the keeper pays 0 (confirmed: `exhaust_budget` and `refuse` both left the attacker’s balance unchanged). `resource_probe.guarded` asserts `round >= next_allowed` and then sets `next_allowed = round + gap`. Anyone can call it.

**Worked example, same numbers as the spike, reproduced.** Base 4,000, cap 40,000, interval 10, gap 6.

| step | result |
|---|---|
| honest `execute` at first due | creator paid **7,600** (LocalNet: the execute txn is itself a block, so every run is one round late; excess=1, extra 3,600) |
| attacker `guarded()` | costs 1,000 |
| honest `execute` | refused, honest paid **0** |
| attacker `execute` once the window reopens | creator paid **25,600** (excess=6) |
| attacker wallet | **+21,600** (25,600 − 1,000 − 3,000 execute fee) |

`poetry run python -m scripts.spike_hostile_target --network localnet` asserts this and exited 0. 25,600 − 4,000 = 21,600 more than base; 21,600 / 1,000 = 21.6×, which they rounded to 22×.

**Economics.** LocalNet hides concurrent keepers: the spike’s honest account tries once, then the attacker waits alone. That would overstate profit if honest keepers retried every round. **The reference bot does the opposite.** `scripts/keeper_backoff.py` treats an inner-tx failure as a broken target, not a lost race (`INNER_FAILURE_MARKER = "inner tx"`). First failure backs off `1 × interval` rounds, capped at 1,286 (~one hour). `select_due` then **omits** that upkeep, and the comment says a rising fee does not fix a broken target. Against `keeper_bot`:

- 10-round interval → silent for 10 rounds; gap is 6; attacker has the window alone
- daily upkeep → silent for an hour

There is no race to win. 21,600 is a lower bound against this client, not an overestimate. I could not construct a case where the attack loses money except “the attacker fails to submit `execute` at all,” which is not a market constraint.

TestNet 2.8 s/round does not save this. The attacker knows `next_allowed` and can sit `first_valid` on that round. Custom keepers that do not back off would split the prize; the bot this repo tells people to run will not be in that split.

**Severity.** Agree it is not a contract-theft bug: every µALGO is inside the `fee_cap` the creator stored, `cancel` still works, CATCH_UP still pays base on replays. Disagree that it is merely advice.

Escalation’s premise — lateness means the job did not clear at base — is false whenever the target can be shut. That is an incentive bug in the feature `#14` just added. `fee_cap = 0` is a workaround that means “do not use the feature.” There is **no method that changes `fee_cap` after `register`**, so the seven live escalating upkeeps cannot take the advice without cancelling and re-registering. The console already defaults the ceiling to 0 and already says “expect to pay it”; `docs/integrating.md` was not updated on this branch.

**Remedy.** `fee_cap = 0` on a blockable target stops the extra payment. It does not stop the liveness half: one inner-tx failure still sends `keeper_bot` away for up to an hour, including on upkeeps with escalation off.

The ramp claim is true. The one-interval ramp is only in `execute` (390–400). The box stores `fee_cap`, `fee_per_execution`, `interval_rounds`, `last_serviced_round`, `policy` — not the formula. `update` can stretch it; `freeze` locks it. Spreading the ramp does **less** than they think, because the reference bot will not “get through once” inside the backoff. A slower ramp cuts the payout per hit; it does not force the attacker to keep paying 1,000 while honest keepers retry.

---

## 2. Worse forms the verification did not measure

Ranked by µALGO I actually moved, then by damage the contract does not meter.

### 2a. One call, every escalating upkeep on that target — 65,800 µALGO

**Path.** `guarded()` writes global `next_allowed`. Every upkeep whose inner call is `guarded()` reads the same slot.

**Measured** (fresh probe, two upkeeps, base 4,000, cap 40,000, gap 6, no warmup): one `guarded()` at due; both honest `execute`s refused; attacker collected **32,800** and **40,000**; wallet **+65,800** after 1,000 + two 3,000 execute fees. Second collection hit the cap because LocalNet serialized the two waits.

Verify: `/tmp/arcron_worse_lateness3.py` (not in the repo). The verification named this (“four live targets already have more than one upkeep”) and did not run it. Profit scales with the count of escalating upkeeps sharing the target, minus 3,000 per `execute`. I did not check how many of the seven live caps share a target.

### 2b. SKIP_AHEAD + cooldown > interval never returns to base — 22,000 µALGO per cycle until escrow is empty

**Path.** Inner `guarded()` on a successful `execute` sets `next_allowed = now + gap`. Under SKIP_AHEAD, `next_due` snaps to the next slot (~one interval out). If `gap > interval`, that slot is still locked, so the next run is always late, `due > last_serviced` stays true, and the fee stays escalated. **No further blocking transaction.**

**Measured** (fresh probe, gap 15, interval 10, SKIP_AHEAD): cycle 0 honest 7,600; cycles 1–5 attacker **22,000** each (excess = 5; `4000 + 36000×5/10`). Five cycles, no extra `guarded()` calls.

CATCH_UP in the same setup is a different failure: two 22,000 hits, then base forever, and the backlog **grows**. Cycle 2: `next=1634, last=1635`; cycle 5: `next=1664, last=1680`. Due advances by 10, the lock by 15. The verification’s “CATCH_UP re-phases… back on base within two cycles” is false here. Their spike used `GUARD_GAP = 6 < INTERVAL = 10`, which is the only configuration in which that sentence holds.

This is exactly the “oracle once an epoch / rebalancer once an epoch” target they used as the motivating example, pointed at an upkeep whose interval is shorter than that epoch.

### 2c. Sibling upkeep as the blocking call — +30,800, and it bypasses “restrict to the keeper app”

**Path.** Attacker `register`s their own upkeep at `fee_cap = 0` against the same method, `execute`s it at due (inner call trips `guarded`), then `execute`s the victim once the window reopens.

**Measured:** sibling paid the attacker **4,000**; honest victim refused; victim paid **32,800**; wallet **+30,800**. The blocking transaction is an earning `execute`, not a 1,000 µALGO app call.

`docs/integrating.md` tells authors to `assert Txn.sender == keeper_app.address`. That stops a raw `guarded()`. It does not stop this: the inner sender is the keeper app either way. Anyone may register a second upkeep on a permissionless registry.

### 2d. Reference-bot silence, including at `fee_cap = 0`

Same path as 1, without collecting. One inner-tx failure → `record_failure` → upkeep dropped from `select_due` for up to 1,286 rounds. For a liquidation, an oracle, a keep-alive, the missed window can be worth more than 21,600 µALGO and the contract will never show it. I did not put a dollar figure on a live target; I did read the backoff code and the scan loop.

### 2e. I1 (fallback decline) composed with 1

When manufactured lateness would hit `if balance < fee: fee = base` (`contract.py` 406–407), a `top_up` of `cap - balance` in the same group lifts the escrow, `execute` pays the cap, escrow goes to 0. The attacker’s top-up returns inside the fee; profit is whatever the box still held, minus fees. The mock test covers the fee arithmetic, not the grouping, and not the composition with a blocked target.

I did not find a way to take **more than `fee_cap` per successful `execute`**, or to touch another upkeep’s escrow.

---

## 3. What both passes missed in the contract itself

I read all eight methods, the router (TEAL 1–140), every `itxn_field Fee` (all `0`), and the payment binds on `register` / `top_up` / `opt_in_asset` / `top_up_asset`. I did not find a new theft path. Reentrancy, sandwich-as-inner-group-of-1, box MBR charge, `due > last_serviced` replay gate, overflow bound on `(cap-base)*excess`, cancel’s best-effort ASA, and “funding must cover the cap” all look as described.

What a self-review would skip, besides §2:

- **`fee_cap` is write-once.** No mutate-upkeep. Immediate advice cannot be applied to existing boxes.
- **`registry_health.read_upkeeps` does not paginate** (`application_boxes(..., limit=1_000)`). `keeper_bot.scan_upkeeps` does. The new solvency sum silently undercounts past 1,000 boxes and reports solvent. Fine at 33. The check they added to close F1 is the one that will lie first as the registry grows.
- **Variable inner-txn cost as a cheaper block.** Honest `keeper_bot` pays `EXTRA_FEE_MICROALGO = 2,000`. A target whose inner-txn count is state-dependent can be pushed over that budget; the bot backs off; the attacker brings more fee and collects. I did not reproduce this on chain. It is the same shape as `guarded` with a different shutter.
- I did not re-run `spike_reentrancy.py` or rebuild F1’s unfunded-keeper table. The F1 arithmetic (14×4,000 from 220,000 → 15th refused at 160,000 vs min 162,100; cancel refused at 37,900 vs min 100,000) is internally consistent.

F1, F2, F3 hold. F1 is Low and they were right to put the watch in `health` rather than in the program.

---

## 4. The diff, as a diff

**`scripts/node_retry.py` — POST retry.** The CDN story is not a sound general argument. `is_refusal` matches any 403/429. algosdk’s `algod_request` raises `AlgodHTTPError(message, code)` and **drops headers**; a Nodely quota 403 and an auth-proxy 403 are indistinguishable. Wrapping is installed on every `connect()`, including LocalNet and any `ALGOD_SERVER`.

The implementation is still safe for duplicate submit, for a reason they did not use: `install` wraps `algod_request`, and `send_raw_transaction` POSTs the already-signed `txn_bytes`. A retry is the same blob. Algorand txn ids are hashes; a second POST is “already in the pool” / “already committed,” not a second payment. That is why POST is safe here, not “the CDN never forwarded it.”

Excluding 5xx is a product choice, not a safety requirement at this layer. A 502 on GET is safe to retry; a 502 on POST of the same blob is too. `retry_after_seconds` looking at `error.headers` is dead against real algosdk errors. The 1-in-15,000 figure is `0.09^4`; the code does five attempts (`0.09^5 ≈ 1 in 169,000`).

**`read_escrowed(..., 1.0, 0)`.** Sound. Those arguments only feed `runway_days` and `rounds_late`, which the sum does not read. Fragile if a later caller uses the `UpkeepHealth` list for anything else. Pagination, above, is the real hole.

**`govern status` solvency line.** Right place. `create` cannot fund an address that does not exist yet. Process still exits 0 on shortfall, which is fine for a status command. The sample `54.201` in `deploying.md` is a snapshot; the audit’s `54.217` is an earlier one. Both can be true hours apart.

**`test_the_fallback_to_base_is_the_keepers_to_decline`.** Proves: escrow below the escalated fee → `execute` pays base; after a **separate** `top_up` of `cap - held`, `execute` pays cap and the box is 0. Does not prove a same-group atomic top-up (a sniper can take a non-grouped top-up). Does not prove 19,400. The last assert is `cap - shortfall == held` with `shortfall = cap - held`, which cannot fail. The 19,400 figure is LocalNet-only and lives in a docstring.

**`spike_hostile_target` in `[lanes.local]`.** It is deterministic enough to stay green, because it **does not assert the finding**. If `guarded` does not hold, or the attacker never gets through, or the three-execute group is refused, the function returns an empty `escapes` and the process exits 0. `measure_atomic_backlog` never asserts `runs == 3`. A lane that “must stay green” will, including when the attack has stopped working. That is the opposite of `keeper_race`, which they correctly left out of the lane because a non-collision proves nothing.

---

## 5. Document errors beyond the two already corrected

The verification was right to leave I1 “no third-party loss” and “two keepers racing produce one payment” unedited in the audit. More:

- **Self-heal within two cycles** is false when the target’s cooldown is longer than the interval. SKIP_AHEAD then farms the escalated fee with no further attacker txns; CATCH_UP then pays base while the backlog grows without bound. I measured both.
- **“The attacker must win the race; honest keepers poll”** is false for `keeper_bot`. They poll, then go away.
- **22×** is 21.6× on one upkeep with no competition. On two upkeeps I measured 65.8×. README presents 22× as the finding.
- **“Advice immediately… in the console and `docs/integrating.md`”** was not done on this branch. Console default is already 0; there is still no warning that a blockable target makes the ceiling a prize.
- **1 − 0.91⁴⁰ ≈ 98%** is 97.7%. Fine. **1 in 15,000** is the wrong exponent.
- F1 table, one-round-late 3,600, six-cycle 49,200 split (21,600 + 3,600), fallback 19,400 with 5,000 of fees, inner `Fee 0`, bracket `group_size` 1: all checked out where I could recompute or re-run.
- I did not establish 7/33, the 8,000–10,000 gap, or four shared targets. Those need TestNet reads this review was not allowed to make.

The audit’s MainNet answer (contract yes, with funded base MBR and a multisig freeze date) still holds as a statement about **theft from the program**. Section 3 of the verification adds a creator-education problem and a freeze-ordering argument for the ramp. The freeze-ordering argument is real and slightly stronger than they said, because SKIP_AHEAD + slow target is a permanent leak of the cap after a single phase shift, and `update` is the only cheap way to flatten the ramp.

---

CONFIDENCE: 52 - that this branch is correct, its two documents are true, and it is safe to merge

BLOCKERS:
Verification §3 "it self-heals… back on base within two cycles" is false when target cooldown > interval (SKIP_AHEAD farms 22,000/cycle with no further attacker txns; CATCH_UP's backlog grows forever) — the motivating example in that section is exactly this shape
Verification §3 "the attacker must win the race; honest keepers poll" is false against keeper_bot: one inner-tx failure backs the upkeep off for 1×interval (capped at 1,286 rounds) and select_due then ignores the rising fee
scripts/spike_hostile_target.py is in [lanes.local] but exits 0 if the attack does not happen (guard did not hold / attacker never got through / backlog group refused); the lane will stay green while the finding is unreproduced
docs/integrating.md and the console fee-cap field were named as the immediate remedy and were not changed; fee_cap cannot be changed on the seven live escalating upkeeps without cancel+re-register, which the documents do not say

---

# Second pass, after the fixes

Re-run the same day against the same branch with the blockers above worked on,
this report handed back so it could mark each one closed, partly closed or
open. **Confidence 52 to 76.** What it found this time was mostly created
by the first round of fixes.

---

I'll start from the three earlier reviews and the response commit, then independently verify each claimed fix rather than taking the commit message at face value.Next I’ll read the other two reviews, the response commit, and the rewritten verification sections.Four commits vs `main` now, not three. I’ll read the response commit and the rewritten verification sections next.I’ll read the claimed code fixes in parallel and start the LocalNet spike so the numbers can be checked against the document.The spike is green, but the composition numbers already disagree with the document. I’ll read the remaining claimed fixes and check whether `CLAUDE.md` was left behind.The pagination mock may not match algosdk’s real parameter names — I’ll check that next, then spot-check tests and the leftover `CLAUDE.md` claim.Pagination may not survive real algosdk. I’ll probe that, then spot-check tests, TestNet reads, and leftover document claims.559 tests collected, matching the claim. I’ll run them, parse the keeper log against the quota arithmetic, and do a TestNet read of the live registry.Checking one pagination detail and whether `1.0.md` still names CEPY as a stranger:Second pass on `f62a223`. `main..HEAD` is **four** commits, not three (`61d9a5a` is still on the branch). I re-read the three reviews and the rewritten verification, read the code, ran the spike twice on LocalNet, ran 559 unit tests and `specsync --strict`, and did a TestNet **read** of app 769891898 (33 upkeeps, 7 escalating, solvency 54.043 == 54.043). Nothing was signed.

## My earlier blockers

**Spike exits 0 if the attack does not happen — CLOSED.** Every empty measurement now returns a failure string; `_send` only treats AVM wording as a refusal and re-raises the rest. I read those paths; a dead node can no longer paint the lane green.

**§3 “self-heals within two cycles” — CLOSED.** That sentence is gone. `measure_no_self_heal` ran twice at `[7600, 22000, 22000, 22000]` with no attacker, SKIP_AHEAD, gap 15 > interval 10, and fails if the later fees all sit at base.

**§3 “the attacker must win a race” — CLOSED.** The rewrite now says `keeper_backoff` treats `inner tx` as a broken target, not a lost race, and `select_due` then omits the upkeep for up to 1,286 rounds. That matches `scripts/keeper_backoff.py` lines 59–77 and 163–164.

**`integrating.md` / write-once `fee_cap` — CLOSED.** The blockable-target warning is there with the measured 25,600 / 45,600 figures, and it says `fee_cap` is fixed at `register`. Console `feeCap` already defaults to 0; that was true this morning.

## The other claimed work

| Claim | Verdict |
|---|---|
| Three worse variants measured **and** asserted | **Mostly.** Cooldown, sibling (4/4, victim 55,600, attacker +45,600), and escrow-drain all ran and fail if they measure nothing. CATCH_UP “backlog grows without bound” is still prose, not in the spike. |
| Spread-the-ramp withdrawn for creator-signed `raise_fee` + freeze ordering | **Yes, that is the right call.** Spreading the ramp does nothing to a cooldown longer than the interval, a sibling blocker, or this repo’s own bot. Replacing automatic escalation with a creator-signed bid, and not freezing while `execute` still pays for manufactured lateness, is the decision that actually follows from what was measured. It is a recommendation, not an implementation. |
| `read_solvency` refuses; `govern status` catches it | **Closed.** Missing `min-balance` raises `ValueError`; `status` logs it and returns 0. |
| `read_upkeeps` paginates via `scan_upkeeps` | **Partly.** It does reuse `scan_upkeeps`. Against real algosdk, `application_boxes(app_id, next=token)` TypeErrors (`algod_request` has no `next`). The new tests mock a client that accepts `next=`. At 33 boxes this never fires; past the node’s box page size, health/govern would crash rather than undercount. Crash is the safer of the two lies, but the tests claim paging works. |
| Fallback test compares two paths | **Closed.** Plain execute pays base and leaves `held - base`; top-up then execute pays cap and leaves 0. The last line is still `cap - shortfall == held` under a new name; it is no longer the finding. |
| `node_retry` txid idempotency, duplicate POST, 5xx-on-reads, arithmetic | **Closed.** Duplicate after a replay returns `{"txId": ...}` so `send_raw_transaction` still gets a string; a duplicate on the first try still raises; 5xx retries GET only; `0.09**5` ≈ 1 in 169,000 is the number the tests and docstring now share. |
| Node-quota attribution inverted | **Closed as reasoning.** The log now has 11,543 scans and 1,996 HTTP 403s from `xyz.corvidlabs.arcron.keeper.testnet`. 11,541 × 37 is **427,017**, not the 429,957 they wrote (~0.7% off). The direction is right: this bot is the counter. |
| AGENTS.md / split.md / status.md | **Closed for those three.** |

TestNet read: **7 of 33** escalating, gaps 8,000 or 10,000, **four** targets with more than one upkeep. Matches. Solvency is 54.043/54.043 today, not the 54.126 in the verification — zero margin is still true, the snapshot moved.

Spot-check, not a full lane: 559 passed, `specsync check --strict` passed, hostile-target passed twice, local lane really is 22 steps and includes `hostile-target`. I did not re-run `fledge lanes run ci` or `local`.

## What the fixes introduced

The pagination tests are the same shape as the spike bug they just closed: they stay green without talking to the client the production path uses. `scan_upkeeps(..., next=token)` TypeErrors on real algosdk. I confirmed that with a probe; the mock never would.

The spike’s own module docstring still says “the registry recovers on its own once the attacker stops,” in the same file as `measure_no_self_heal`. That sentence is now false inside the evidence.

Escrow-drain numbers are a LocalNet snapshot, not a pin: first run printed held 28,800 / top-up 11,200 / net +23,800 (first execute two rounds late); second run printed the document’s 32,400 / 7,600 / +27,400. The assertions are properties (`left == 0`, `net > base`), so the lane is honest; the verification’s “Measured: 32,400 … 27,400” is one of the two outcomes.

## Still wrong in the documents

`CLAUDE.md` still says “26 of the 33 live upkeeps now belong to somebody else,” and it is the file that tells agents to keep AGENTS.md / README / CLAUDE.md in sync. `docs/design/1.0.md` still says CEPY52VZRWFL is a stranger and still gates MainNet on “outside creators.” `split.md` even notes that `1.0.md` should be rewritten and then doesn’t. That is #105 on the two files that matter most for agents and for the gate.

Also leftover: verification §1 still says “508 unit tests pass”; the spike still logs the 7,600 run as “on schedule”; `11,541 × 37` is not 429,957.

Confidence moved from 52 because the four things I blocked on were actually done. It is not in the 80s because they claimed the sibling technique and missed `CLAUDE.md` and `1.0.md`, and because the new pagination tests do not test algosdk.

CONFIDENCE: 76 - that this branch is correct, its documents are true, and it is safe to merge
BLOCKERS: NONE

---

# Third pass, on the two items left open

The liveness fix and the request-load fix, reviewed the same day they landed.
**Confidence 84.** All three passes were asked for an honest number rather
than an agreeable one, and this one was also asked whether 95 is the right bar
for a repository that has produced a previously undiscovered true-sounding
claim in every round including this one.

---

I'll start by reading the review skill and the two prior reports, then inspect the claimed liveness and request-load fixes without changing anything.Prior reports are in. Next I’ll inspect the two claimed-close commits, then attack the liveness classifier and request-load cache independently.The two commits are in; next I’ll read the classifier, cache, and contract `execute` path and attack them against the claimed invariants.Classifier and cache look plausible on the surface; next I’ll check `execute`’s inner-txn order, how failures are recorded, and whether the request-count test is measuring the real client.I have the cache and classifier paths; next I’ll read the remaining edges, then run tests and a LocalNet spike while checking TestNet read-only.Recorded algod messages match the classifier. Next I’ll run the test suite, a LocalNet spike, and a TestNet read of upkeep 91 and the live targets.578 unit tests passed. I’ll print the actual request counts, then start the CI lane and a TestNet read.CI is green (14 steps). Next I’ll run the 22-step local lane, which includes the hostile-target spike and the e2e classifier check against real algod.Checking the e2e log for the on-chain classifier assertion, then I’ll write the verdict.On `fix/keeper-liveness-and-load` (`807de55`). Tree clean. 578 passed. `fledge lanes run ci` 14/14. `fledge lanes run local` 22/22, including hostile-target (25,600 / +21,600; no-self-heal `[7600, 22000, 22000, 22000]`; sibling 4/4, 55,600, +45,600). TestNet read-only: 32 upkeeps, **91 gone**, 7 escalating.

**1. Liveness — CLOSED.** A real LocalNet `err` in the target came back `inner tx 0 failed` at `app=1004 pc=92`; e2e asserts `next_attempt_round == 101` and `target_refusal is True`. `execute` asserts due/escrow before any `itxn`; recorded `Not due` messages have no `inner tx` and still take the race path. A target cannot hide an inner-program refusal as keeper-side. 64 is a product choice (and is further capped at the upkeep’s own interval, so the 20-round cadences never wait 64). An attacker still buys ~3 minutes exclusive at ~20 refusals/hour, and the sibling-upkeep variant still gets paid to do it; that is the residual they named, not the hour-from-one-assert. Clearing the streak on `advanced=True` / success does not open a new lever.

**2. Request load — CLOSED.** `next_execution_round` is written at `register` as `round+interval` and only advanced by `execute`; `top_up` only adds balance; `update` does not touch boxes. A cached “not due until X” cannot hide a due funded upkeep. I re-ran `TestWhatOneDayCosts`: **5,901 requests / 4,713 reading / 594 executions** over 63,013 rounds — the digit, not a band. `CountingAlgod(AlgodClient)` rejects `next=` kwargs the real client would TypeError on. It is a model of the 2026-09-01 snapshot (always SKIP_AHEAD; wall-clock `MAX_SLEEP_SECONDS` never fires), not production traffic, but it is counting the production call sites.

**3. Reclaim creator check — CLOSED.** Preview prices only `creator == deployer.address`; TestNet no longer has box 91. `--commit` still iterates `found`, so mixed `--upkeep` still *attempts* the others; they fail, the number no longer lies.

**4. Verification §3 / §5 — CLOSED** as asked (`**Since fixed.**`; 37-not-36 and 5,901). **§7 still says the liveness question was not answered.** Same sibling-sweep failure as last pass.

### What these fixes broke or introduced

- **The classifier is not the partition the docstring describes.** `inner tx 1 failed` (payment / bonus after a successful target call) is treated as a target refusal — safe direction, faster F1 retry. The reverse exists: a failure *before* the inner program starts has no marker. `smoke-reference-boundary` on this machine logged `dynamic cost budget exceeded … at PC 1487` and `tx references exceed MaxAppTotalTxnReferences = 8` with **no** `inner tx`; those take the old interval/hour schedule. A target that is a resource bomb, not an `assert`, can still buy the hour.
- A due starved upkeep that is topped up is invisible for up to `STARVED_RECHECK_ROUNDS` (an hour). A brand-new registration can sit due for up to `MAX_IDLE_ROUNDS` (128). Both are written down; both mean “nothing due can be missed” is stronger than the code.
- GitHub `keeper-bot.yml` is still `*/30 * * * * --once` with no backoff memory. It never had the hour bug and still does not contest a 64-round reopen.
- `LIVE_REGISTRY` in the measurement still includes cancelled 91. Right for the §5 window; stale as a picture of today (32 boxes).

95 is the wrong bar for this repository. This round’s new fact is the pre-inner classification hole, which does not restore the original blocker and is not enough to keep the branch closed. What would move this number is §7 matching §3, and an on-chain test that `inner tx 0` / `inner tx 1` / pre-inner budget-or-refs are three cases.

CONFIDENCE: 84 - that this work is correct, its documents are true, and it is safe to merge
BLOCKERS: NONE
